{
  "name": "bun-cli-example",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build --minify --compile ./index.ts  --outfile mycli  --external terminal-kit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/qrcode": "^1.5.5",
    "@types/terminal-kit": "^2.5.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "otplib": "^12.0.1",
    "qrcode": "^1.5.4",
    "terminal-kit": "^3.1.2"
  }
}